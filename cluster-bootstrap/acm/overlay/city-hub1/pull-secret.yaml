# ---
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: multiclusterhub-operator-pull-secret
#   namespace: multicluster-engine
# spec:
#   refreshInterval: "15s"
#   secretStoreRef:
#     name: vault-backend
#     kind: ClusterSecretStore
#   target:
#     name: multiclusterhub-operator-pull-secret
#   data:
#   - secretKey: .dockerconfigjson
#     remoteRef:
#       key: secret/multiclusterhub-operator-pull-secret
#       property: .dockerconfigjson

---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: multiclusterhub-operator-pull-secret
  namespace: open-cluster-management
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: multiclusterhub-operator-pull-secret
  data:
  - secretKey: .dockerconfigjson
    remoteRef:
      key: secret/multiclusterhub-operator-pull-secret
      property: .dockerconfigjson
